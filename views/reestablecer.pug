doctype
html(lang='en')
    head
        include partials/header
        link(rel="stylesheet", href="/css/Sesion.css")
    body.login
        .contenedor-formulario
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            .container.login-container
                .row
                    .col-md-6.login-form-2
                        h3 Agrega tu e-mail para restablecer la contraseña
                        form(method="POST" action="/reestablecer")
                            .form-group
                                input.form-control(type="email", name="email" placeholder="Tu email" required 
                                    value= email)
                            .form-group
                                input.btn.btn-success(type="submit" value="Reestablecer Password")
                            .form-group
                                a.ForgetPwd(href="/iniciar-sesion") Ya lo recorde volver a login
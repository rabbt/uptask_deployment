<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
        link(rel="stylesheet", href="/css/Sesion.css")
    body.login

        .contenedor-formulario
            h1 Crea tu cuenta en la app

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            .container.login-container
                .row
                    .col-md-6.login-form-2
                        h3 Crear cuenta        
                        form(method="POST" action ="/crear-cuenta")
                            .form-group   
                                input.form-control(type="email", name="email" placeholder="Tu email" required 
                                value= email)

                            .form-group
                                input.form-control(type="password", name="password" placeholder="password" required
                                value= password)

                            .form-group
                                input.btn.btn-success(type="submit" value="Crear Cuenta")
                            .form-group
                                a.ForgetPwd(href="/iniciar-sesion") Volver a Iniciar Sesion


